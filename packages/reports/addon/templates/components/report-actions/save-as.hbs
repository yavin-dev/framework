{{!-- Copyright 2020, Yahoo Holdings Inc. Licensed under the terms of the MIT license. See accompanying LICENSE.md file for terms. --}}
<DenaliModal @isOpen={{this.showModal}} @onClose={{this.closeModal}} ...attributes as |modal|>
  <modal.Header>
    <div class="primary-header">Save Report As</div>
  </modal.Header>
  <modal.Content>
    <div class="secondary-header">This will create a new report with the new given name.</div>
    {{!-- input for new Name of the Report --}}
    <DenaliInput
      class="input dashboard-title m-b-30"
      @placeholder={{this.reportName}}
      @value={{this.reportName}}
      {{on "input" (pipe (pick 'target.value') (fn (mut this.reportName)))}}
    />

    {{!-- Buttons --}}
    <div class="btn-container">
      {{!-- Create a new copy of the report --}}
      <LinkTo @route="reports.report.save-as" @query={{hash title=this.reportName}}
        @tagName="button"
        class="button is-solid save-as__save-as-modal-btn m-5"
      >
        Save
      </LinkTo>
      <DenaliButton class="save-as__cancel-modal-btn m-5" @style="outline" {{on "click" this.closeModal}}>Cancel</DenaliButton>
    </div>
  </modal.Content>
</DenaliModal>
