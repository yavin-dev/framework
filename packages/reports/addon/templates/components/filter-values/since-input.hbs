{{!-- Copyright 2020, Yahoo Holdings Inc. Licensed under the terms of the MIT license. See accompanying LICENSE.md file for terms. --}}
{{#if @isCollapsed}}
  {{#let (as-interval this.interval) as |interval|}}
    {{#if interval}}
      {{this.dateRange}}
    {{else}}
      <FilterValues::InvalidValue/>
    {{/if}}
  {{/let}}
{{else}}
  <div class="filter-values--since-input" ...attributes>
    <DropdownDatePicker
      class="filter-values--since-input__trigger filter-values--since-input__low-value"
      @date={{this.startDate}}
      @dateTimePeriod={{this.dateTimePeriod}}
      @onUpdate={{action this.setIntervalStart}}
    >
      {{#if this.startDate}}
        {{this.startFormat}}
      {{else}}
        <span class="date__placeholder">{{this.startPlaceholder}}</span>
      {{/if}}
    </DropdownDatePicker>

    {{#let (eq this.interval._end "next") as |isNext|}}
      <div class="filter-values--since-input__separator">
        <Input
          @type="checkbox"
          @id="currentDateTimePeriod"
          @checked={{isNext}}
          {{on "click" (fn this.setIntervalEnd (if isNext "current" "next"))}}
        />
        <label for="currentDateTimePeriod">Include this {{this.dateTimePeriod}}?</label>
      </div>
    {{/let}}
  </div>
{{/if}}