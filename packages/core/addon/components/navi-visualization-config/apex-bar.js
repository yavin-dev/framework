/**
 * Usage:
 * <NaviVisualizationConfig::ApexBar
 *   @request=request
 *   @response=response
 *   @options=tableOptions
 * />
 */
import Component from '@ember/component';
import layout from '../../templates/components/navi-visualization-config/apex-bar';

export default Component.extend({
  layout
});
