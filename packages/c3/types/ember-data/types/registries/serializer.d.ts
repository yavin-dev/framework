/**
 * There seems to be a problem with nested linked packaged
 * So defining our own local registry
 */
// eslint-disable-next-line @typescript-eslint/no-empty-interface
export default interface SerializerRegistry {}
